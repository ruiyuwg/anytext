Parameter

Type

Description

Example

object

The containerd runtime configuration.

registryMirrors

array

The image accelerator configuration.

string

The URL of the image accelerator.

https://example.com

insecureRegistries

array

Insecure image repositories that skipped the TLS certificate verification.

string

The URLs of image repositories that skipped the TLS certificate verification.

example.com
